grumphp:
    tasks:
        phpunit:
            config_file: phpunit.xml
            coverage-clover: 'build/logs/clover.xml'
        shell:
            scripts:
                - ["-c", "/var/www/html/vendor/bin/coverage-check /var/www/html/build/logs/clover.xml 80"]
        # phplint: ~
        # behat: ~
        
        # phpcs:
        #     standard:
        #         - 'PSR12'

